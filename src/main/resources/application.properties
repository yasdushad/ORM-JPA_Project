spring.application.name=Learnig-Spring-ORM

#Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/boot_course
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver=com.mysql.cj.jdbc.Driver

#JPA/Hibernete Configuration

# none means it is not performing any operation
#spring.jpa.hibernate.ddl-auto=none

#Create table but at the time of session end it is not dropping any table
# spring.jpa.hibernate.ddl-auto=create

#Create table but at the time of session end it is not dropping any table
#spring.jpa.hibernate.ddl-auto=create-drop

# it means it is running required scheme like table not creating it is creating table but already exit will not run
spring.jpa.hibernate.ddl-auto=update

#it is logging which is generated by hibernate
Spring.jpa.show-sql=true

#dialect :
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

#keep the connection  alive if idle for a long time (needed in production)
#spring.datasource.testWhileIdle=true
#spring.datasource.validationQuery=SELECT 1
